# odin-recipes

The website will consist of a main index page which will have links to a few recipes.

In this project I'd demostrate my HTML skill that I learned in TOP.

Those skilss are:
Create the boilerplate with an "!", as simple like that. It contains on the top the HTML5 format then is setted up the language in english, then the character encoding in utf-8, then the responsive design for smartphones, tablets and very kind of device.

Then the title, it's the name that is seen on the top in the tab bar it gives the main website name and make it easier to find it on google 

Then is the body that contains all the links, images, text,etc

Was learned in this HTML section all kind of commands; for paragraph, titles, links, bold text, italic text, subtitles, links, images and all the attribues that comes with it

